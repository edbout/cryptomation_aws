source "https://rubygems.org"

# Sinatra core and helpers
gem "sinatra"
gem "sinatra-contrib", require: "sinatra/reloader"
gem "rack"

# For `CGI.escapeHTML`
gem "cgi"   # ships with Ruby, but Bundler likes it declared

# If you want to go via `rackup` / puma later
gem "puma", group: :production